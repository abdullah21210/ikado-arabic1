// نموذج نهائي لمحاكي أوتوكاد + Office عربي متكامل (2D + Word + Excel + PowerPoint + حفظ + طبقات + PNG/PDF + 3D + ذكاء + نشر ويب + صوت + اقتراحات + مكتبة أشكال + إعدادات + حماية بكلمة مرور + تسجيل دخول)

import React, { useEffect, useRef, useState } from "react";
import { fabric } from "fabric";
import * as THREE from "three";
import jsPDF from "jspdf";

// ... (بقية الشيفرة مأخوذة من الكانفاس الذي تم حفظه)
